const fs = require('fs');

// Base64 encoded simple fire icon PNGs (just an approximation or we can just fetch an emoji PNG)
// I will just download a free fire emoji from a public URL or use a unicode emoji on a canvas.
